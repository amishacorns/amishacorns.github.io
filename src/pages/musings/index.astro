---
import { getCollection } from "astro:content"
import PageLayout from "@layouts/PageLayout.astro"
import TopLayout from "@layouts/TopLayout.astro"
import BottomLayout from "@layouts/BottomLayout.astro"
import { BLOG } from "@consts"
import { formatDate } from "@lib/utils"

const posts = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
---

<PageLayout title={BLOG.TITLE} description={BLOG.DESCRIPTION}>
  <TopLayout>
    <div class="animate max-w-2xl">
      <p class="text-xs font-semibold uppercase tracking-[0.22em] text-violet-300/65">Signals from the edge</p>
      <h1 class="mt-3 text-4xl font-semibold text-white">Musings</h1>
      <p class="mt-3 text-lg text-white/55">{BLOG.DESCRIPTION}</p>
    </div>
  </TopLayout>
  <BottomLayout>
    <div class="animate grid gap-5 md:grid-cols-2">
      {posts.map((post, index) => (
        <a class:list={["musing-card", index === 0 && "md:col-span-2 md:grid md:grid-cols-2"]} href={post.data.permalink}>
          {post.data.header?.teaser && (
            <div class:list={["musing-image", index === 0 ? "md:aspect-[16/9]" : ""]}>
              <img src={`/images/${post.data.header.teaser}`} alt="" loading="lazy" />
            </div>
          )}
          <div class="p-5 md:p-6 flex flex-col justify-between">
            <div>
              <div class="flex flex-wrap items-center gap-2 text-xs uppercase tracking-[0.12em] text-white/40">
                <span>{formatDate(post.data.date)}</span>
                <span>·</span>
                <span>{post.data.tags[0]}</span>
              </div>
              <h2 class:list={["mt-3 font-semibold text-white", index === 0 ? "text-2xl" : "text-xl"]}>{post.data.title}</h2>
              <p class="mt-3 leading-relaxed text-white/55">{post.data.description}</p>
            </div>
            <span class="mt-6 text-sm text-violet-200/70">Read transmission →</span>
          </div>
        </a>
      ))}
    </div>
  </BottomLayout>
</PageLayout>

<style>
  .musing-card { @apply overflow-hidden rounded-2xl border border-white/15 bg-white/[0.025] hover:-translate-y-1 hover:border-violet-300/40 hover:bg-white/[0.055] transition-all duration-300; }
  .musing-image { @apply aspect-[16/10] overflow-hidden border-b border-white/10 bg-white/5; }
  .musing-image img { @apply h-full w-full object-cover opacity-75 grayscale-[10%] transition duration-500; }
  .musing-card:hover img { @apply scale-[1.025] opacity-100 grayscale-0; }
</style>
